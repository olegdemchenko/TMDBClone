import { css } from '@emotion/react';

export const emptyContainerStyles = css({
  height: 300,
});

export const rowStyles = css({
  display: 'flex',
  flexWrap: 'nowrap',
  overflowY: 'hidden',
});
