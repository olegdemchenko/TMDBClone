import { css } from '@emotion/react';

export const modalStyles = css({
  '& .modal-content': {
    backgroundColor: 'transparent',
    alignItems: 'center',
    border: 'none',
  },
});

export const spinnerSizeStyles = css({
  width: '5rem',
  height: '5rem',
});
